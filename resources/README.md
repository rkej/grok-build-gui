# Packaged app resources

- `icon.png` — generated by `npm run icon` (`scripts/write-icon.mjs`). Do not hand-edit.
- `entitlements.mac.plist` / `entitlements.mac.inherit.plist` — unsigned local
  macOS builds. Notarization is off until a signing identity exists.
